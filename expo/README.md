# RN Alarm App (migrated from Expo)

Smart alarm assistant app.

## Setup
- `npm install`
- For Android: `./gradlew assembleDebug` in `./android`
- Run: `npx react-native run-android`

## Features
- ...
